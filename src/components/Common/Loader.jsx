// eslint-disable-next-line no-unused-vars
import React from "react";
import "./Loader.css"; // Optional: Import any styles

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader; // Default export
